# @nativescript/android-quickjs

NativeScript runtime package for Android, built with the QuickJS JavaScript engine.

## Build

From the repo root:

```sh
./gradlew -Pengine=QUICKJS
```

This produces the npm artifact in `dist_quickjs/`.
